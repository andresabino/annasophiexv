import type pg from 'pg';const blocked=new Set(['password','password_hash','token','token_hash','token_ciphertext','secret','connection_string']);function clean(value:any):any{if(Array.isArray(value))return value.map(clean);if(value&&typeof value==='object')return Object.fromEntries(Object.entries(value).filter(([k])=>!blocked.has(k.toLowerCase())).map(([k,v])=>[k,clean(v)]));return value;}export async function audit(client:pg.PoolClient,userId:number,action:string,entityType:string,entityId:number|null,before:any=null,after:any=null){await client.query('INSERT INTO audit_log(admin_user_id,action,entity_type,entity_id,before_data,after_data) VALUES($1,$2,$3,$4,$5,$6)',[userId,action,entityType,entityId,clean(before),clean(after)]);}
