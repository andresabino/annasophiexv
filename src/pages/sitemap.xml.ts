import type {APIRoute} from 'astro';import {siteUrl,lifecycleConfig} from '../lib/config';import {getCurrentSitePhase,isGiftListActive} from '../lib/lifecycle';
export const GET:APIRoute=async()=>{const c=await lifecycleConfig();const post=getCurrentSitePhase(c)==='POST_EVENT';const paths=post?['/','/fotos','/obrigado']:['/','/save-the-date','/fotos',...(isGiftListActive(c)?['/presentes']:[])];return new Response('<?xml version="1.0" encoding="UTF-8"?><urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">'+paths.map(p=>'<url><loc>'+new URL(p,siteUrl()).href.replaceAll('&','&amp;')+'</loc></url>').join('')+'</urlset>',{headers:{'Content-Type':'application/xml'}})};